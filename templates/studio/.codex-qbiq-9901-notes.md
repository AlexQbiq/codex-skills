QBIQ-9901 / circulation collapse e2e notes

- Disposable test design: `design_result_id = 909833`
- Initial immutable reset snapshot: `69c1a9b564ec30834a82a601`
- Test can mutate the live design and rely on autosave creating a new snapshot
- Preferred test flow:
  1. Reset active design state for `909833` from snapshot `69c1a9b564ec30834a82a601`
  2. Open `http://localhost:3000/studio/account/4917/workspace/4918/testfit/24006/design/909833`
  3. Preload auth/localStorage state before page load
  4. Click the `Select circulation` toolbar button by test id
  5. Perform the exact canvas drag by coordinates
  6. Assert the autosaved `PATCH /api/v2/studio/909833/state` result
- Why disposable is preferred over undo:
  - avoids undo-stack coupling
  - avoids autosave racing with Ctrl+Z
  - makes runs repeatable from a known state
